#include <iostream>
#include <fstream> 

using namespace std;

int t[1000];
{
   for (int i=0; i<100; i++)
   
   {
   
   t[i]=0;
   
}

for (int i=0; i<100; i++)
plik>>t[i++];

 {
 

 ifstream plik;
    plik.open("c:\\zadaniee\\a.txt");  
    if(plik.good())  
     while(!plik.eof())       
                         {
                         plik>>a;  
                         cout<<a;    
                         cout<<endl; 
                         } 
    plik.close();
    for (int j=100-1; 1>=0; j--)
 {
 	t[j]=1
 }
 
   return 0;
